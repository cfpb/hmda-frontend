import React from 'react'
import './StackedProgressBars.css'

const StackedProgressBars = (props) => (
  <div className='stacked-progress-bars'>{props.children}</div>
)

export default StackedProgressBars
